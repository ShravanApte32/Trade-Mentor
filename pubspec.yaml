name: trading_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Existing dependencies - Keeping all your current ones
  http: 1.6.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  candlesticks: ^0.3.0
  interactive_chart: ^0.3.6
  syncfusion_flutter_charts: ^25.2.4
  cupertino_icons: ^1.0.8
  
  # NEW UI Enhancement Packages (add these)
  google_fonts: ^6.1.0          # Professional fonts
  shimmer: ^3.0.0               # Loading animations
  flutter_staggered_animations: ^1.1.1  # Smooth animations
  smooth_page_indicator: ^1.1.0 # Page indicators
  cached_network_image: ^3.3.0  # Image caching
  flutter_svg: ^2.0.9           # SVG icons support
  provider: ^6.1.1              # State management

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Add assets for icons and images (create these folders)
  # assets:
  #   - assets/icons/
  #   - assets/images/